# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1113 Screen Capture (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0346 Detect Screen Capture via Commands and API Calls
# Analytic      : AN0980
# Telemetry     : Sysmon EventCode 1 -> process_creation/windows
# Quality       : weak - matches broad activity - suitable for threat hunting, not alerting (only values common in normal activity (\powershell.exe))
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 2 executables, 2 tools
#
# ATT&CK tuning knobs for this analytic:
#   - ParentProcessName: Depends on allowed parent process behaviors in the environment (e.g., explorer.exe vs...
#   - TimeWindow: Can tune alert thresholds for rapid or scheduled screenshots (e.g., interval-based screen...
#   - ImageExtension: To detect file writes (e.g., .bmp, .png) that deviate from typical user activity
#
# Notes:
#   - Weak rule: only values common in normal activity (\powershell.exe).
#     It is tagged detection.threat-hunting and capped at level low - use
#     it to hunt, or add the constraint that separates the technique from
#     normal activity. Where ATT&CK describes volume, --chains adds a
#     count-based version.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Screen Capture Activity Via Process Creation
id: cdeee036-5e5f-5506-af36-f4b599f62f94
status: experimental
description: |
    Draft detection for T1113 (Screen Capture), derived from ATT&CK detection strategy DET0346 /
    analytic AN0980.
    Adversaries may attempt to take screen captures of the desktop to gather information over the
    course of an operation. Screen capturing functionality may be included as a feature of a remote
    access tool used in post-compromise operations.
    ATT&CK analytic: Unusual use of screen capture APIs (e.g., CopyFromScreen) or command-line tools
    to write image files to disk.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1113
    - https://attack.mitre.org/detectionstrategies/DET0346
    - https://attack.mitre.org/detectionstrategies/DET0346#AN0980
    - https://docs.microsoft.com/en-us/dotnet/api/system.drawing.graphics.copyfromscreen?view=netframework-4.8
    - https://blog.malwarebytes.com/threat-analysis/2017/01/new-mac-backdoor-using-antiquated-code/
author: sigma-generator
date: 2026-09-24
tags:
    - attack.collection
    - attack.t1113
    - detection.threat-hunting
logsource:
    category: process_creation
    product: windows
detection:
    selection_image:
        Image|endswith:
            - '\powershell.exe'
    condition: selection_image
fields:
    - Image
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - Legitimate administrative or software-deployment use of powershell.exe
    - 'Environment-specific tuning of ParentProcessName: Depends on allowed parent process behaviors in the environment (e.g., explorer.exe vs powershell.exe)'
    - 'Environment-specific tuning of TimeWindow: Can tune alert thresholds for rapid or scheduled screenshots (e.g., interval-based screen capture)'
    - 'Environment-specific tuning of ImageExtension: To detect file writes (e.g., .bmp, .png) that deviate from typical user activity'
    - Unknown
level: low
