# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1016.001 Internet Connection Discovery (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0357 Behavioral Detection of Internet Connection Discovery
# Analytic      : AN1015
# Telemetry     : Sysmon EventCode 1 -> process_creation/windows
# Quality       : weak - matches broad activity - suitable for threat hunting, not alerting (rests on a single value (Test-NetConnection))
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 1 cmdlets
#
# ATT&CK tuning knobs for this analytic:
#   - DestinationIP: Tunable external IP ranges or domains used to verify Internet access (e.g., 8.8.8.8...
#   - TimeWindow: Cluster rapid test connections with command execution in < 60 seconds
#   - UserContext: Filter out known admin/script contexts to reduce false positives
#
# Notes:
#   - Weak rule: rests on a single value (Test-NetConnection). It is
#     tagged detection.threat-hunting and capped at level low - use it to
#     hunt, or add the constraint that separates the technique from normal
#     activity. Where ATT&CK describes volume, --chains adds a count-based
#     version.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Internet Connection Discovery Activity Via Process Creation
id: 56f56d20-1d65-5dd3-9063-2f16f7c968f2
status: experimental
description: |
    Draft detection for T1016.001 (Internet Connection Discovery), derived from ATT&CK detection
    strategy DET0357 / analytic AN1015.
    Adversaries may check for Internet connectivity on compromised systems. This may be performed
    during automated discovery and can be accomplished in numerous ways such as using Ping, tracert,
    and GET requests to websites, or performing initial speed testing to confirm bandwidth.
    ATT&CK analytic: Execution of utilities (e.g., ping, tracert, Test-NetConnection) or scripted
    methods to test Internet connectivity by interacting with external IPs/domains.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1016/001
    - https://attack.mitre.org/detectionstrategies/DET0357
    - https://attack.mitre.org/detectionstrategies/DET0357#AN1015
author: sigma-generator
date: 2026-09-24
tags:
    - attack.discovery
    - attack.t1016
    - attack.t1016.001
    - detection.threat-hunting
logsource:
    category: process_creation
    product: windows
detection:
    selection_indicator_content:
        CommandLine|contains:
            - Test-NetConnection
    condition: selection_indicator_content
fields:
    - Image
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - 'Environment-specific tuning of DestinationIP: Tunable external IP ranges or domains used to verify Internet access (e.g., 8.8.8.8, example.com)'
    - 'Environment-specific tuning of TimeWindow: Cluster rapid test connections with command execution in < 60 seconds'
    - 'Environment-specific tuning of UserContext: Filter out known admin/script contexts to reduce false positives'
    - Unknown
level: low
