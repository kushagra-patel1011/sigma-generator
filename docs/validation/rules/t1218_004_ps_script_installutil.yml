# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1218.004 InstallUtil (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0138 Detection of Malicious Code Execution via InstallUtil.exe
# Analytic      : AN0388
# Telemetry     : PowerShell EventID 4104 -> ps_script/windows
# Quality       : moderate - one behavioural signal - review for noise (2 specific values in one selection)
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 4 executables, 3 tools, 1 paths, 1 api calls
#
# ATT&CK tuning knobs for this analytic:
#   - InstallUtilPathRegex: Regex pattern for InstallUtil.exe in .NET directories; tune to exclude known good...
#   - AssemblyPathRegex: Patterns for identifying suspicious assemblies (e.g., in temp folders, user profiles)
#   - ChildProcessList: List of suspicious child processes spawned from InstallUtil.exe (e.g., cmd.exe...
#   - TimeWindow: Time correlation window between file creation of assembly and its execution via...
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential InstallUtil Activity Via PowerShell Script Block Logging
id: 2e161ae2-3775-5889-92fd-345f350e9464
status: experimental
description: |
    Draft detection for T1218.004 (InstallUtil), derived from ATT&CK detection strategy DET0138 /
    analytic AN0388.
    Adversaries may use InstallUtil to proxy execution of code through a trusted Windows utility.
    InstallUtil is a command-line utility that allows for installation and uninstallation of
    resources by executing specific installer components specified in .NET binaries.
    ATT&CK analytic: Execution of InstallUtil.exe from .NET framework directories with arguments
    specifying non-standard or attacker-supplied assemblies, especially when followed by suspicious
    child process creation or script execution. Detection also includes correlation of newly created
    binaries prior to InstallUtil invocation and anomalous command-line usage compared to historical
    baselines.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1218/004
    - https://attack.mitre.org/detectionstrategies/DET0138
    - https://attack.mitre.org/detectionstrategies/DET0138#AN0388
    - https://lolbas-project.github.io/lolbas/Binaries/Installutil/
    - https://msdn.microsoft.com/en-us/library/50614e95.aspx
author: sigma-generator
date: 2026-09-24
tags:
    - attack.stealth
    - attack.t1218
    - attack.t1218.004
logsource:
    category: ps_script
    product: windows
    definition: Script block logging must be enabled
detection:
    selection_indicator_content:
        ScriptBlockText|contains:
            - ':\Windows\Microsoft.NET\Framework\v\InstallUtil.exe'
            - RunInstaller
    condition: selection_indicator_content
fields:
    - ScriptBlockText
    - UserId
    - Computer
falsepositives:
    - 'Environment-specific tuning of InstallUtilPathRegex: Regex pattern for InstallUtil.exe in .NET directories; tune to exclude known good administrative scripts'
    - 'Environment-specific tuning of AssemblyPathRegex: Patterns for identifying suspicious assemblies (e.g., in temp folders, user profiles)'
    - 'Environment-specific tuning of ChildProcessList: List of suspicious child processes spawned from InstallUtil.exe (e.g., cmd.exe, powershell.exe, rundll32.exe)'
    - Unknown
level: low
