# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1689 Downgrade Attack (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0350 Detecting Downgrade Attacks
# Analytic      : AN0995
# Telemetry     : Sysmon EventCode 1 -> process_creation/windows
# Quality       : weak - matches broad activity - suitable for threat hunting, not alerting (only values common in normal activity (\powershell.exe))
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 1 tools
#
# ATT&CK tuning knobs for this analytic:
#   - AllowedInterpreterVersions: Defines which versions of interpreters like PowerShell are permitted in the environment.
#   - RegistryDefenderKeys: Specific registry paths for monitoring Defender/HVCI configurations that may vary by...
#
# Notes:
#   - Weak rule: only values common in normal activity (\powershell.exe).
#     It is tagged detection.threat-hunting and capped at level low - use
#     it to hunt, or add the constraint that separates the technique from
#     normal activity. Where ATT&CK describes volume, --chains adds a
#     count-based version.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Downgrade Attack Activity Via Process Creation
id: a59e284d-6967-5add-ab93-1708ad0b3708
status: experimental
description: |
    Draft detection for T1689 (Downgrade Attack), derived from ATT&CK detection strategy DET0350 /
    analytic AN0995.
    Adversaries may downgrade or use a version of system features that may be outdated, vulnerable,
    and/or does not support updated security controls. Downgrade attacks typically take advantage of
    a system’s backward compatibility to force it into less secure modes of operation.
    ATT&CK analytic: Detection of processes launching downgraded PowerShell versions (e.g., v2) or
    other legacy binaries that lack logging or security features. Correlates command-line arguments,
    process metadata, and version fields.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1689
    - https://attack.mitre.org/detectionstrategies/DET0350
    - https://attack.mitre.org/detectionstrategies/DET0350#AN0995
    - https://www.safebreach.com/blog/downgrade-attacks-using-windows-updates/
    - https://www.crowdstrike.com/en-us/cybersecurity-101/cyberattacks/downgrade-attack/
    - https://blog.checkpoint.com/research/targeted-ssl-stripping-attacks-are-real/amp/
author: sigma-generator
date: 2026-09-24
tags:
    - attack.defense-impairment
    - attack.t1689
    - detection.threat-hunting
logsource:
    category: process_creation
    product: windows
detection:
    selection_image:
        Image|endswith:
            - '\powershell.exe'
    condition: selection_image
fields:
    - Image
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - Legitimate administrative or software-deployment use of powershell.exe
    - 'Environment-specific tuning of AllowedInterpreterVersions: Defines which versions of interpreters like PowerShell are permitted in the environment.'
    - 'Environment-specific tuning of RegistryDefenderKeys: Specific registry paths for monitoring Defender/HVCI configurations that may vary by Windows version.'
    - Unknown
level: low
