# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1095 Non-Application Layer Protocol (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0457 Detection of Non-Application Layer Protocols for C2
# Analytic      : AN1254
# Telemetry     : Sysmon EventCode 3 -> network_connection/windows
# Quality       : moderate - one behavioural signal - review for noise (2 specific values in one selection)
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 1 executables, 3 tools
#
# ATT&CK tuning knobs for this analytic:
#   - ProcessContextAllowList: Processes normally allowed to use ICMP/UDP (e.g., ping.exe, DNS resolver).
#   - ByteTransferAnomalyThreshold: Suspicion if client sends much more data than it receives (e.g., >90%).
#   - ProtocolUsageBaseline: Baseline which protocols are normal per host or segment (ICMP, UDP, etc.).
#
# Notes:
#   - ATT&CK also cites Sysmon EventCode 22 for this analytic - consider a
#     companion rule.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Non-Application Layer Protocol Activity Via Network Connection
id: 9239133a-710f-57b5-995f-929337d2728a
status: experimental
description: |
    Draft detection for T1095 (Non-Application Layer Protocol), derived from ATT&CK detection
    strategy DET0457 / analytic AN1254.
    Adversaries may use an OSI non-application layer protocol for communication between host and C2
    server or among infected hosts within a network. The list of possible protocols is extensive.
    ATT&CK analytic: Anomalous use of ICMP or UDP by non-network service processes for data
    exfiltration or remote control, especially if traffic bypasses proxy infrastructure or shows
    unusual flow patterns.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1095
    - https://attack.mitre.org/detectionstrategies/DET0457
    - https://attack.mitre.org/detectionstrategies/DET0457#AN1254
    - https://cloud.google.com/blog/topics/threat-intelligence/vmware-esxi-zero-day-bypass/
    - https://arxiv.org/ftp/arxiv/papers/1408/1408.1136.pdf
    - https://blogs.cisco.com/security/evolution-of-attacks-on-cisco-ios-devices
author: sigma-generator
date: 2026-09-24
tags:
    - attack.command-and-control
    - attack.t1095
logsource:
    category: network_connection
    product: windows
detection:
    selection_source:
        Initiated: true
    selection_image:
        Image|endswith:
            - '\netstat.exe'
            - '\ping.exe'
    condition: selection_source and selection_image
fields:
    - Image
    - DestinationIp
    - DestinationPort
    - DestinationHostname
    - User
falsepositives:
    - Legitimate administrative or software-deployment use of netstat.exe, ping.exe
    - 'Environment-specific tuning of ProcessContextAllowList: Processes normally allowed to use ICMP/UDP (e.g., ping.exe, DNS resolver).'
    - 'Environment-specific tuning of ByteTransferAnomalyThreshold: Suspicion if client sends much more data than it receives (e.g., >90%).'
    - 'Environment-specific tuning of ProtocolUsageBaseline: Baseline which protocols are normal per host or segment (ICMP, UDP, etc.).'
    - Unknown
level: low
