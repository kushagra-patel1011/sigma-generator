# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1574.001 DLL (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0201 Detection Strategy for Hijack Execution Flow for DLLs
# Analytic      : AN0577
# Telemetry     : Sysmon EventCode 11 -> file_event/windows
# Quality       : weak - matches broad activity - suitable for threat hunting, not alerting (only values common in normal activity (:\Windows\System32))
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 1 paths
#
# ATT&CK tuning knobs for this analytic:
#   - AllowedDllPaths: Known safe DLL directories to suppress false positives (e.g., C:\Windows\System32).
#   - ProcessAllowList: Applications expected to load DLLs from non-standard locations (e.g., development tools).
#   - TimeWindow: Correlation interval between DLL file creation, registry changes, and module load.
#   - HashBaseline: Baseline hashes for legitimate DLLs used to detect substitution.
#
# Notes:
#   - Weak rule: only values common in normal activity
#     (:\Windows\System32). It is tagged detection.threat-hunting and
#     capped at level low - use it to hunt, or add the constraint that
#     separates the technique from normal activity. Where ATT&CK describes
#     volume, --chains adds a count-based version.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential DLL Activity Via File Creation
id: fd2b80bb-4b7c-5d0c-b09c-74f2dd6aa242
status: experimental
description: |
    Draft detection for T1574.001 (DLL), derived from ATT&CK detection strategy DET0201 / analytic
    AN0577.
    Adversaries may abuse dynamic-link library files (DLLs) in order to achieve persistence,
    escalate privileges, and evade defenses. DLLs are libraries that contain code and data that can
    be simultaneously utilized by multiple programs.
    ATT&CK analytic: DLL hijacking behaviors including unexpected DLL loads from non-standard
    directories, replacement of DLLs, phantom DLL insertion, redirection file creation, and
    substitution of legitimate DLLs. Defender correlates file system modifications, registry
    changes, and module load telemetry to detect abnormal DLL behavior in trusted processes.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1574/001
    - https://attack.mitre.org/detectionstrategies/DET0201
    - https://attack.mitre.org/detectionstrategies/DET0201#AN0577
    - https://www.crowdstrike.com/en-us/blog/4-ways-adversaries-hijack-dlls/
    - https://www.kroll.com/en/insights/publications/cyber/idatloader-distribution
    - https://news.sophos.com/en-us/2023/05/03/doubled-dll-sideloading-dragon-breath/
author: sigma-generator
date: 2026-09-24
tags:
    - attack.stealth
    - attack.execution
    - attack.t1574
    - attack.t1574.001
    - detection.threat-hunting
logsource:
    category: file_event
    product: windows
detection:
    selection_path:
        TargetFilename|contains:
            - ':\Windows\System32'
    condition: selection_path
fields:
    - Image
    - TargetFilename
    - User
falsepositives:
    - 'Environment-specific tuning of AllowedDllPaths: Known safe DLL directories to suppress false positives (e.g., C:\Windows\System32).'
    - 'Environment-specific tuning of ProcessAllowList: Applications expected to load DLLs from non-standard locations (e.g., development tools).'
    - 'Environment-specific tuning of TimeWindow: Correlation interval between DLL file creation, registry changes, and module load.'
    - Unknown
level: low
