# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1518 Software Discovery (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0392 Multi-Platform Software Discovery Behavior Chain
# Analytic      : AN1100
# Telemetry     : Windows Security (4688 -> process_creation) -> process_creation/windows
# Quality       : weak - matches broad activity - suitable for threat hunting, not alerting (only values common in normal activity (\powershell.exe))
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 1 tools
#
# ATT&CK tuning knobs for this analytic:
#   - TimeWindow: Detection may be scoped to multiple discovery commands within a short timeframe.
#   - ParentProcess: Tuning based on whether discovery activity stems from suspicious versus approved...
#
# Notes:
#   - Security 4688 exposes NewProcessName/ProcessCommandLine natively;
#     the process_creation category normalises them to Image/CommandLine.
#   - Weak rule: only values common in normal activity (\powershell.exe).
#     It is tagged detection.threat-hunting and capped at level low - use
#     it to hunt, or add the constraint that separates the technique from
#     normal activity. Where ATT&CK describes volume, --chains adds a
#     count-based version.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Software Discovery Activity Via Process Creation
id: 8d55cdb8-ca30-5e54-ba09-5a371b9efb76
status: experimental
description: |
    Draft detection for T1518 (Software Discovery), derived from ATT&CK detection strategy DET0392 /
    analytic AN1100.
    Adversaries may attempt to get a listing of software and software versions that are installed on
    a system or in a cloud environment. Adversaries may use the information from Software Discovery
    during automated discovery to shape follow-on behaviors, including whether or not the adversary
    fully infects the target and/or attempts specific actions.
    ATT&CK analytic: Adversary spawns a process or script to enumerate installed software using WMI,
    registry, or PowerShell, potentially followed by additional discovery or evasion behavior.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1518
    - https://attack.mitre.org/detectionstrategies/DET0392
    - https://attack.mitre.org/detectionstrategies/DET0392#AN1100
author: sigma-generator
date: 2026-09-24
tags:
    - attack.discovery
    - attack.t1518
    - detection.threat-hunting
logsource:
    category: process_creation
    product: windows
detection:
    selection_image:
        Image|endswith:
            - '\powershell.exe'
    condition: selection_image
fields:
    - Image
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - Legitimate administrative or software-deployment use of powershell.exe
    - 'Environment-specific tuning of TimeWindow: Detection may be scoped to multiple discovery commands within a short timeframe.'
    - 'Environment-specific tuning of ParentProcess: Tuning based on whether discovery activity stems from suspicious versus approved management tools.'
    - Unknown
level: low
