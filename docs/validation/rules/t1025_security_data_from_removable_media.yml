# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1025 Data from Removable Media (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0511 Detection of Data Access and Collection from Removable Media
# Analytic      : AN1410
# Telemetry     : Windows Security (EventID 4663, 4670, 4656) -> windows/security
# Quality       : weak - matches broad activity - suitable for threat hunting, not alerting (only the event type is selected - every such event matches)
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : none - ATT&CK contained no field-level artefacts
#
# ATT&CK tuning knobs for this analytic:
#   - VolumeLabel: Can tune based on known removable device labels or whitelist
#   - TimeWindow: Controls timing between device mount and sensitive file access
#   - TargetFileType: Tune for sensitive file extensions (e.g., .docx, .pdf, .csv)
#
# Notes:
#   - Weak rule: only the event type is selected - every such event
#     matches. It is tagged detection.threat-hunting and capped at level
#     low - use it to hunt, or add the constraint that separates the
#     technique from normal activity. Where ATT&CK describes volume,
#     --chains adds a count-based version.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Data from Removable Media Activity Via Windows Security
id: 6a9a1ad5-579a-5f20-82ab-004ace547783
status: experimental
description: |
    Draft detection for T1025 (Data from Removable Media), derived from ATT&CK detection strategy
    DET0511 / analytic AN1410.
    Adversaries may search connected removable media on computers they have compromised to find
    files of interest. Sensitive data can be collected from any removable media (optical disk drive,
    USB memory, etc.) connected to the compromised system prior to Exfiltration.
    ATT&CK analytic: Adversary mounts a USB device and begins enumerating, copying, or compressing
    files using scripting engines, cmd, or remote access tools.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1025
    - https://attack.mitre.org/detectionstrategies/DET0511
    - https://attack.mitre.org/detectionstrategies/DET0511#AN1410
author: sigma-generator
date: 2026-09-24
tags:
    - attack.collection
    - attack.t1025
    - detection.threat-hunting
logsource:
    product: windows
    service: security
detection:
    selection_source:
        EventID:
            - 4663
            - 4670
            - 4656
    condition: selection_source
fields:
    - EventID
    - ObjectName
    - ObjectType
    - AccessMask
    - ProcessName
    - SubjectUserName
falsepositives:
    - 'Environment-specific tuning of VolumeLabel: Can tune based on known removable device labels or whitelist'
    - 'Environment-specific tuning of TimeWindow: Controls timing between device mount and sensitive file access'
    - 'Environment-specific tuning of TargetFileType: Tune for sensitive file extensions (e.g., .docx, .pdf, .csv)'
    - Unknown
level: low
