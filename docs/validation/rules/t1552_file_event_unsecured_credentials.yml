# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1552 Unsecured Credentials (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0412 Detect Access or Search for Unsecured Credentials Across Platforms
# Analytic      : AN1153
# Telemetry     : Sysmon EventCode 11 -> file_event/windows
# Quality       : weak - matches broad activity - suitable for threat hunting, not alerting (rests on a single value (\reg.exe))
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 3 executables, 1 tools
#
# ATT&CK tuning knobs for this analytic:
#   - TimeWindow: Defines the threshold time for accessing multiple sensitive files indicating automation.
#   - SuspiciousProcessList: Process names to monitor (e.g., reg.exe, cmd.exe, powershell.exe, etc.)
#
# Notes:
#   - Left out \cmd.exe, \powershell.exe: common in normal activity, and
#     in a list of alternatives each value matches on its own. Add them
#     back only together with a narrower condition.
#   - Weak rule: rests on a single value (\reg.exe). It is tagged
#     detection.threat-hunting and capped at level low - use it to hunt,
#     or add the constraint that separates the technique from normal
#     activity. Where ATT&CK describes volume, --chains adds a count-based
#     version.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Unsecured Credentials Activity Via File Creation
id: 82115d40-f8ba-57a7-a3fe-43fa1db5548e
status: experimental
description: |
    Draft detection for T1552 (Unsecured Credentials), derived from ATT&CK detection strategy
    DET0412 / analytic AN1153.
    Adversaries may search compromised systems to find and obtain insecurely stored credentials.
    These credentials can be stored and/or misplaced in many locations on a system, including
    plaintext files (e.g. Shell History), operating system or application-specific repositories
    (e.g. Credentials in Registry), or other specialized files/artifacts (e.g. Private Keys).
    ATT&CK analytic: Unusual access to bash history, registry credentials paths, or private key
    files by unauthorized or scripting tools, with correlated file and process activity.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1552
    - https://attack.mitre.org/detectionstrategies/DET0412
    - https://attack.mitre.org/detectionstrategies/DET0412#AN1153
    - https://labs.portcullis.co.uk/download/eu-18-Wadhwa-Brown-Where-2-worlds-collide-Bringing-Mimikatz-et-al-to-UNIX.pdf
author: sigma-generator
date: 2026-09-24
tags:
    - attack.credential-access
    - attack.t1552
    - detection.threat-hunting
logsource:
    category: file_event
    product: windows
detection:
    selection_image:
        Image|endswith:
            - '\reg.exe'
    condition: selection_image
fields:
    - Image
    - TargetFilename
    - User
falsepositives:
    - Legitimate administrative or software-deployment use of reg.exe
    - 'Environment-specific tuning of TimeWindow: Defines the threshold time for accessing multiple sensitive files indicating automation.'
    - 'Environment-specific tuning of SuspiciousProcessList: Process names to monitor (e.g., reg.exe, cmd.exe, powershell.exe, etc.)'
    - Unknown
level: low
