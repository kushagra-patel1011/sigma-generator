# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1546.008 Accessibility Features (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0033 Detection Strategy for Accessibility Feature Hijacking via Binary Replacement or Registry Modification
# Analytic      : AN0094
# Telemetry     : Sysmon EventCode 11 -> file_event/windows
# Quality       : moderate - one behavioural signal - review for noise (a subject selection)
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 8 executables, 1 tools, 7 paths
#
# ATT&CK tuning knobs for this analytic:
#   - TimeWindow: Time between registry modification and suspicious binary execution (e.g., < 1 hour) can...
#   - TargetBinaryNames: Specific binaries monitored (e.g., utilman.exe, sethc.exe) can be adjusted per OS version...
#   - ParentProcess: Parent process of cmd.exe (e.g., winlogon.exe) may vary across legitimate and adversarial...
#   - UserContext: Context of SYSTEM account execution vs. administrative sessions may influence tuning.
#   - CommandLineContains: Tunable patterns such as launching cmd.exe, powershell, or LOLBins from accessibility...
#
# Notes:
#   - ATT&CK also names cmd.exe, displayswitch.exe, magnify.exe,
#     narrator.exe; they were not required here because the change can be
#     made by any process. A companion process_creation rule is the place
#     for them.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Accessibility Features Activity Via File Creation
id: 3f5e8d5c-ae0b-5ae8-b93a-de01940bae14
status: experimental
description: |
    Draft detection for T1546.008 (Accessibility Features), derived from ATT&CK detection strategy
    DET0033 / analytic AN0094.
    Adversaries may establish persistence and/or elevate privileges by executing malicious content
    triggered by accessibility features. Windows contains accessibility features that may be
    launched with a key combination before a user has logged in (ex: when the user is on the Windows
    logon screen).
    ATT&CK analytic: Defenders can observe suspicious replacement or tampering of system
    accessibility binaries (e.g., utilman.exe, sethc.exe, osk.exe) and anomalous modifications to
    registry keys used to redirect accessibility programs (such as IFEO keys). Additionally,
    execution of cmd.exe or other suspicious binaries triggered from the login screen by SYSTEM can
    be correlated as part of a...
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1546/008
    - https://attack.mitre.org/detectionstrategies/DET0033
    - https://attack.mitre.org/detectionstrategies/DET0033#AN0094
    - https://giuliocomi.blogspot.com/2019/10/abusing-windows-10-narrators-feedback.html
    - https://web.archive.org/web/20190216180458/https://www.fireeye.com/blog/threat-research/2012/08/hikit-rootkit-advanced-persistent-attack-techniques-part-1.html
    - https://www.slideshare.net/DennisMaldonado5/sticky-keys-to-the-kingdom
author: sigma-generator
date: 2026-09-24
tags:
    - attack.privilege-escalation
    - attack.persistence
    - attack.t1546
    - attack.t1546.008
logsource:
    category: file_event
    product: windows
detection:
    selection_path:
        TargetFilename|contains:
            - ':\Windows\System32\AtBroker.exe'
            - ':\Windows\System32\DisplaySwitch.exe'
            - ':\Windows\System32\Magnify.exe'
            - ':\Windows\System32\Narrator.exe'
            - ':\Windows\System32\osk.exe'
            - ':\Windows\System32\sethc.exe'
            - ':\Windows\System32\utilman.exe'
    condition: selection_path
fields:
    - Image
    - TargetFilename
    - User
falsepositives:
    - 'Environment-specific tuning of TimeWindow: Time between registry modification and suspicious binary execution (e.g., < 1 hour) can be tuned.'
    - 'Environment-specific tuning of TargetBinaryNames: Specific binaries monitored (e.g., utilman.exe, sethc.exe) can be adjusted per OS version and risk tolerance.'
    - 'Environment-specific tuning of ParentProcess: Parent process of cmd.exe (e.g., winlogon.exe) may vary across legitimate and adversarial cases.'
    - Unknown
level: medium
