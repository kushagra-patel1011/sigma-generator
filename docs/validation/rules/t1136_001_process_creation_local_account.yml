# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1136.001 Local Account (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0447 T1136.001 Detection Strategy - Local Account Creation Across Platforms
# Analytic      : AN1235
# Telemetry     : Sysmon EventCode 1 -> process_creation/windows
# Quality       : moderate - one behavioural signal - review for noise (2 specific values in one selection)
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 4 executables, 6 tools
#
# ATT&CK tuning knobs for this analytic:
#   - ParentProcessName: Attackers may use cmd.exe, wscript.exe, or renamed binaries to evade detection
#   - TimeWindow: Define time threshold between process start and user creation event (e.g., 5s–2m)
#   - UserContext: Correlate if process runs under SYSTEM, Administrator, or untrusted account
#
# Notes:
#   - Left out \cmd.exe, \powershell.exe: common in normal activity, and
#     in a list of alternatives each value matches on its own. Add them
#     back only together with a narrower condition.
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Local Account Activity Via Process Creation
id: 49de6a3a-8c00-578c-8cce-aa8e0cc2e00b
status: experimental
description: |
    Draft detection for T1136.001 (Local Account), derived from ATT&CK detection strategy DET0447 /
    analytic AN1235.
    Adversaries may create a local account to maintain access to victim systems. Local accounts are
    those configured by an organization for use by users, remote support, services, or for
    administration on a single system or service.
    ATT&CK analytic: Adversary uses built-in tools like 'net user /add', PowerShell, or WMI to
    create a local user. Sequence: Account creation event (4720) follows process creation of a
    suspicious executable (e.g., powershell.exe or net.exe).
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1136/001
    - https://attack.mitre.org/detectionstrategies/DET0447
    - https://attack.mitre.org/detectionstrategies/DET0447#AN1235
    - https://www.cisco.com/c/en/us/td/docs/ios-xml/ios/security/s1/sec-s1-cr-book/sec-cr-t2.html#wp1047035630
    - https://cybersecuritynews.com/superblack-actors-exploiting-two-fortinet-vulnerabilities/
    - https://kubernetes.io/docs/concepts/security/service-accounts/
author: sigma-generator
date: 2026-09-24
tags:
    - attack.persistence
    - attack.t1136
    - attack.t1136.001
logsource:
    category: process_creation
    product: windows
detection:
    selection_image:
        Image|endswith:
            - '\net.exe'
            - '\wscript.exe'
    condition: selection_image
fields:
    - Image
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - Legitimate administrative or software-deployment use of net.exe, wscript.exe
    - 'Environment-specific tuning of ParentProcessName: Attackers may use cmd.exe, wscript.exe, or renamed binaries to evade detection'
    - 'Environment-specific tuning of TimeWindow: Define time threshold between process start and user creation event (e.g., 5s–2m)'
    - 'Environment-specific tuning of UserContext: Correlate if process runs under SYSTEM, Administrator, or untrusted account'
    - Unknown
level: low
