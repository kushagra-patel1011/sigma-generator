# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.2.0 on 2026-09-24
# --------------------------------------------------------------------------
# ATT&CK        : T1216.002 SyncAppvPublishingServer (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0440 Detecting PowerShell Execution via SyncAppvPublishingServer.vbs Proxy Abuse
# Analytic      : AN1220
# Telemetry     : Sysmon EventCode 1 -> process_creation/windows
# Quality       : strong - two or more independent behavioural signals (2 independent behavioural selections)
# Confidence    : 0.95 - high (ATT&CK named the exact event)
# Mined content : 5 executables, 3 tools, 2 flags
#
# ATT&CK tuning knobs for this analytic:
#   - CommandLineRegex: Detects embedded PowerShell commands in SyncAppvPublishingServer.vbs invocation, e.g....
#   - ScriptInterpreter: May vary between `wscript.exe`, `cscript.exe`, or called via `cmd.exe`
#   - PowerShellObfuscationScore: Used to detect encoding, obfuscation, or entropy level in embedded PowerShell payloads
#   - TimeWindow: Time delta between VBScript proxy invocation and PowerShell payload execution
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential SyncAppvPublishingServer Activity Via Process Creation
id: 57c3dfe5-808b-5314-9f5c-cdbd4b920761
status: experimental
description: |
    Draft detection for T1216.002 (SyncAppvPublishingServer), derived from ATT&CK detection strategy
    DET0440 / analytic AN1220.
    Adversaries may abuse SyncAppvPublishingServer.vbs to proxy execution of malicious PowerShell
    commands. SyncAppvPublishingServer.vbs is a Visual Basic script associated with how Windows
    virtualizes applications (Microsoft Application Virtualization, or App-V).
    ATT&CK analytic: Execution of SyncAppvPublishingServer.vbs through wscript.exe with a command-
    line containing embedded PowerShell, proxying malicious PowerShell execution through a
    Microsoft-signed VBScript interpreter to evade detection and restrictions.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1216/002
    - https://attack.mitre.org/detectionstrategies/DET0440
    - https://attack.mitre.org/detectionstrategies/DET0440#AN1220
    - https://www.trellix.com/en-ca/about/newsroom/stories/research/suspected-darkhotel-apt-activity-update/
    - https://learn.microsoft.com/en-us/windows/application-management/app-v/appv-getting-started
    - https://lolbas-project.github.io/lolbas/Scripts/Syncappvpublishingserver/
author: sigma-generator
date: 2026-09-24
tags:
    - attack.stealth
    - attack.t1216
    - attack.t1216.002
logsource:
    category: process_creation
    product: windows
detection:
    selection_image:
        Image|endswith:
            - '\cmd.exe'
            - '\cscript.exe'
            - '\powershell.exe'
            - '\wscript.exe'
    selection_indicator_content:
        CommandLine|contains:
            - -enc
            - -nop
            - syncappvpublishingserver.vbs
    condition: selection_image and selection_indicator_content
fields:
    - Image
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - Legitimate administrative or software-deployment use of cmd.exe, cscript.exe, powershell.exe
    - 'Environment-specific tuning of CommandLineRegex: Detects embedded PowerShell commands in SyncAppvPublishingServer.vbs invocation, e.g., `{powershell -nop -enc...'
    - 'Environment-specific tuning of ScriptInterpreter: May vary between `wscript.exe`, `cscript.exe`, or called via `cmd.exe`'
    - 'Environment-specific tuning of PowerShellObfuscationScore: Used to detect encoding, obfuscation, or entropy level in embedded PowerShell payloads'
    - Unknown
level: medium
