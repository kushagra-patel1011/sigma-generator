# --------------------------------------------------------------------------
# DRAFT RULE - generated by sigma-generator v1.1.0 on 2026-09-17
# --------------------------------------------------------------------------
# ATT&CK        : T1021.006 Windows Remote Management (ATT&CK v19.2)
# Platform      : Windows
# Strategy      : DET0477 Behavioral Detection of WinRM-Based Remote Access
# Analytic      : AN1313
# Telemetry     : network security monitoring (flow) -> zeek/conn
# Quality       : moderate - one behavioural signal - review for noise (a subject selection)
# Confidence    : 0.75 - medium (ATT&CK named the log source, not the event)
# Mined content : 1 tools, 2 ports
#
# ATT&CK tuning knobs for this analytic:
#   - TimeWindow: Defines max time between remote shell creation and child process execution (e.g., 60...
#   - UserContext: Scope to unexpected remote user logons (non-admins, service accounts)
#   - CommandLineAnomalyScore: Score for suspicious command usage via WinRM (e.g., encoded PowerShell)
#   - KnownAdminHosts: List of trusted systems allowed to use WinRM legitimately
#
# Before deploying:
#   1. Check the field names against your own pipeline / field mappings.
#   2. Replace mined values with artefacts you have actually observed.
#   3. Add environment filters, then measure the false-positive rate.
#   4. Set `status: test` once validated, and re-issue the rule `id` if you fork it.
# --------------------------------------------------------------------------
title: Potential Windows Remote Management Activity Via Zeek Conn
id: f9a1728a-2d37-50cc-adec-3087cdccdce4
status: experimental
description: |
    Draft detection for T1021.006 (Windows Remote Management), derived from ATT&CK detection
    strategy DET0477 / analytic AN1313.
    Adversaries may use Valid Accounts to interact with remote systems using Windows Remote
    Management (WinRM). The adversary may then perform actions as the logged-on user.
    ATT&CK analytic: Adversaries using WinRM to remotely execute commands, launch child processes,
    or access WMI. The detection chain includes service use, network activity, remote session logon,
    and process creation within a short temporal window.
    Generated draft - the selection values below were mined from ATT&CK and must be validated
    against real telemetry before this rule is deployed.
references:
    - https://attack.mitre.org/techniques/T1021/006
    - https://attack.mitre.org/detectionstrategies/DET0477
    - https://attack.mitre.org/detectionstrategies/DET0477#AN1313
    - https://medium.com/threatpunter/detecting-lateral-movement-using-sysmon-and-splunk-318d3be141bc
    - https://www.slideshare.net/kieranjacobsen/lateral-movement-with-power-shell-2
    - https://msdn.microsoft.com/en-us/library/aa394582.aspx
author: sigma-generator
date: 2026-09-17
tags:
    - attack.lateral-movement
    - attack.t1021
    - attack.t1021.006
logsource:
    product: zeek
    service: conn
detection:
    selection_port:
        id.resp_p:
            - 5985
            - 5986
    condition: selection_port
fields:
    - id.orig_h
    - id.resp_h
    - id.resp_p
    - proto
    - service
falsepositives:
    - 'Environment-specific tuning of TimeWindow: Defines max time between remote shell creation and child process execution (e.g., 60 seconds)'
    - 'Environment-specific tuning of UserContext: Scope to unexpected remote user logons (non-admins, service accounts)'
    - 'Environment-specific tuning of CommandLineAnomalyScore: Score for suspicious command usage via WinRM (e.g., encoded PowerShell)'
    - Unknown
level: medium
